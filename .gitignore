# dependencies (npm workspaces hoist to the root)
node_modules/

# build output
extension/dist/
web/.next/
web/out/
web/coverage/
*.tsbuildinfo

# env files / local secrets — never commit real values, .env.example is fine
.env
.env.*
!.env.example
extension/config.json

# misc
.DS_Store
*.pem
.vercel
npm-debug.log*
.neon
